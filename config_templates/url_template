
from django.conf.urls import patterns, include, url

urlpatterns += [
    url(r'^accounts/', include('induwo_registration.urls')),
    url(r'^accounts/', include('registration.backends.hmac.urls')),
    url('^', include('django.contrib.auth.urls')),
]

